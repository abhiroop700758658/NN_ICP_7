# Saving the the model and printing the first few predictions
model. save("improved_iris_model.h5")
from tensorflow.keras.models import load_ saved
_model
_model = load model ("improved _iris_model.h5")
prent redictions model, predict (X test scaled)
print(predictions[:5])